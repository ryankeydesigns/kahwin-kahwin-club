"use client";import { Send,Sparkles,Trash2 } from "lucide-react";import { useMemo,useState } from "react";import { PageHero,SiteFooter,SiteHeader } from "@/components/site-shell";
const qs=["注册需要什么文件？","怎样预约注册？","注册费用是多少？","有什么日期可以注册？","有摄影师推荐吗？","有什么婚纱配套？","附近有什么婚宴餐厅？","有什么专属优惠？"];
export default function Assistant(){const [message,setMessage]=useState(""),[history,setHistory]=useState<{role:string,text:string}[]>([{role:"ai",text:"您好 ♡ 我是 Khawin-Khawin AI Wedding Assistant。我可以帮您了解注册程序、寻找适合日期，以及推荐婚礼服务。"}]);const suggested=useMemo(()=>[...qs].sort(()=>Math.random()-.5).slice(0,6),[]);function ask(q?:string){const v=(q??message).trim();if(!v)return;let a="这项资料目前还没有在系统资料库中，建议直接联系陈氏书院婚姻注册中心确认。";if(v.includes("日期"))a="您可以进入良辰吉日页面，按年份、月份与平日／周末偏好筛选。系统只使用管理员确认的日期资料。";if(v.includes("摄影")||v.includes("婚纱")||v.includes("餐厅"))a="可以 ♡ 请告诉我预算、日期及地区，系统将从合作商家资料中筛选适合的服务和专属优惠。";setHistory(h=>[...h,{role:"user",text:v},{role:"ai",text:a}]);setMessage("")}return <main><SiteHeader/><PageHero eyebrow="ROMANTIC WEDDING AI" title="AI Wedding Assistant"><p>有什么关于注册结婚想了解？</p></PageHero><section className="assistant assistantpage section"><div className="chatwrap"><div className="chatintro"><div className="avatar"><Sparkles/></div><div><b>Khawin-Khawin AI</b><small>在线 · 随时为您解答</small></div><button className="clearbtn" onClick={()=>setHistory(history.slice(0,1))}><Trash2 size={15}/> 清除对话</button></div><div className="chatstream">{history.map((x,i)=><div key={i} className={`bubble ${x.role}`}>{x.text}</div>)}</div><p className="suggestlabel">您也可以这样问：</p><div className="suggestions">{suggested.map(q=><button key={q} onClick={()=>ask(q)}>{q}</button>)}</div><div className="inputbar"><input value={message} onChange={e=>setMessage(e.target.value)} onKeyDown={e=>e.key==="Enter"&&ask()} placeholder="问问我们的 Wedding Assistant…"/><button aria-label="发送" onClick={()=>ask()}><Send size={18}/></button></div><p className="safety">资料只依据注册中心与合作商家确认的内容，不会自行编造费用、条例或优惠。</p></div></section><SiteFooter/></main>}
